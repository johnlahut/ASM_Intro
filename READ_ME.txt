Open the command prompt, (leave it open)

In explorer, Create a folder at the level your command prompt defaults to 

*********MUST BE LOCAL************NO NETWORK BASED FOLDERS****************

	Copy:
		link.exe
		ML.exe
		ML.err
		?.asm   ?=filename of asm source file you want to work with

	into the folder.

At the command prompt, enter the folder you created before. 
   (This is the one with all of your files from above)

assemble the program by typing:
	ml ?.asm 

run your .exe file by typing the name of your asm file w/o the extension:
	?  
	or say 
	welcome


  
